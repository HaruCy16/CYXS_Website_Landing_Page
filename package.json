{
  "scripts": {
    "build": "postcss styles.css -o dist/styles.css",
    "watch": "postcss styles.css -o dist/styles.css --watch",
    "dev": "live-server"
  },
  "dependencies": {
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "postcss-cli": "^11.0.1"
  }
}
