{
  "scripts": {
    "build": "rm -rf public && mkdir -p public/dist && cp index.html public/index.html && cp -R assets public/assets && postcss styles.css -o dist/styles.css && cp dist/styles.css public/dist/styles.css",
    "watch": "postcss styles.css -o dist/styles.css --watch",
    "dev": "live-server"
  },
  "dependencies": {
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "postcss-cli": "^11.0.1"
  }
}
